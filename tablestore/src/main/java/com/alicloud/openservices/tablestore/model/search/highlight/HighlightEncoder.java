package com.alicloud.openservices.tablestore.model.search.highlight;

/**
 * The encoding mode for highlighting the original shard text. By default, no encoding is applied.
 */
public enum HighlightEncoder {
    PLAIN,
    HTML
}
